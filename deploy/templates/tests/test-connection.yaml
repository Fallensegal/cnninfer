apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cnninfer.fullname" . }}-test-connection"
  labels:
    {{- include "cnninfer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cnninfer.fullname" . }}:{{ .Values.webapp.service.port }}/healthz']
  restartPolicy: Never
